rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 포트폴리오(works): 공개 읽기, 관리자(로그인)만 쓰기
    match /works/{workId} {
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }
  }
}
